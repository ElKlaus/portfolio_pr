extends ../base

block head
    title parallax page

block content
    #parallax.parallax
        - for (var i = 1; i <= 10; i++)
            .parallax__layer
                img(src=`./images/layer-${i}.png` id=`layer${i}`).parallax__image